# Examples

Finished, gate-passed outputs committed as reference examples. Unlike
everything in `outputs/` (git-ignored working material), files here have
cleared both gates:

1. Stage 4 identifiability risk check: **PASS at Low risk**
2. `validate_outputs.py` mechanical scan: **clean**

- `composite__recurring-patterns__draft02.md` — fictionalized anonymized
  composite built from 12 themes that recurred across all 3 source
  interviews, with its full audit trail (pattern basis, invented
  connective tissue, exclusions, revision notes).
- `risk_check__recurring-patterns__draft02.md` — the PASS risk-check
  record for that composite.

Only composites that pass both gates belong in this folder. Never commit
transcripts, prepared chunks, pattern maps, or synthesis maps — those stay
in the git-ignored working folders.
